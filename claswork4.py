import random

X = (float(input("what you want to be the maximun ")))
def random():
    a =random.randint(0,X)
    b =random.randint(0,X)


    choice = (input("what problem you want to use + - *"))

    if  (choice=="x",(a*b)):
    if (choice=="-",(a-b)):
    else: (choice==(a+b))


